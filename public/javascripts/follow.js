const followButtonEventListener = (req) => {
  const followButton = document.querySelector("./follow-button");
  followButton.addEventListener("click", (event) => {});
};

// create a follow
// const followUser = (req) => {
//   const followButton = document.querySelector("./follow-button");
//   followButton.addEventListener();
// };
